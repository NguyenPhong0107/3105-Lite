name: 3105Lite
targets:
  3105Lite:
    type: application
    platform: iOS
    deploymentTarget: "16.0"
    sources: [3105Lite]
    settings:
      CODE_SIGN_STYLE: Automatic
    # Thêm đoạn này để chỉ định file Info.plist có sẵn:
    info:
      path: 3105Lite/App/Info.plist
      properties:
        CFBundleShortVersionString: "1.0"
        CFBundleVersion: "1"
